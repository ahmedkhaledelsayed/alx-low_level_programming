#include "main.h"

/**
* _puts - function that returns the length of a string
*@str: pointer to string
*/

void _puts(char *str)
{
	int index, counter = 0;

	for (index = 0; s[index]; index++)
	{
		printf("%s", s[index]);
	}
}
