#include "main.h"

/**
 * _atoi -  a function that convert a string that
 * reprsents an integer into an actual integer data type.
 * @s: pointer to an string.
 *
 * Return: int
*/

int _atoi(char *s)
{
	int i = 98;

	return (i);
}
